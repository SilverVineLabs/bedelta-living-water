# V1.5 PoC — Aave/Morpho Fallback & Variational Hedge Adapter Stubs

> **Vitest SSOT:** 173 test files | 765 PASS Clean

> Aave/Morpho in this log: *(Hurdle-rate probe only — not a yield-stacking product track)*.

**Date:** 2026-08-27  
**Branch:** `v1.5_roadmap`  
**Phase:** V1.5 Tiered Liquidity Stacking + Variational Native Short Leg  
**Constraint:** 零破壞性變更 — 未修改 `pkg/soil_core.wasm`、`src/services/risk-control.ts`、`SliverVineGate.sol`

---

## 摘要

於 `src/services/adapters/` 新增兩個隔離式 PoC adapter stub，實作階梯式流動性退守（Aave v3 / Morpho Blue）與 Variational 同鏈 1× Short 對沖載荷建構。所有邏輯為純 TypeScript、可注入基準 APY，尚未接線至 hot-path 或 Wasm。

---

## 新增模組

| 檔案 | 行數 | 職責 |
|------|------|------|
| `aave-morpho-fallback-adapter.ts` | 123 | `checkTieredFallbackCondition()` · `routeToRiskFreeBase()` |
| `variational-hedge-adapter.ts` | 139 | `buildVariationalShortOrder()` · `evaluateVariationalOrderbookDepth()` |

---

## `aave-morpho-fallback-adapter.ts`

### `checkTieredFallbackCondition()`

觸發條件（任一成立即 `shouldFallback: true`）：

1. **Soil Fail-Closed** — `soilFailClosed === true` · `soilOk === false` · `signingChannelOpen === false`
2. **GMX Skew Rebate < 0.5%** — `gmxSkewRebateBps < 50`（50 bps 門檻）

### `routeToRiskFreeBase()`

| 條件 | 路由目標 | 基準 APY |
|------|----------|----------|
| `aaveSupplyCapUtilization < 1.0` | Aave v3 USDC Pool (`0x794a…814aD`) | 4.0% ~ 5.2%（`clamp` 至 `DEFAULT_AAVE_BASE_APY`） |
| `aaveSupplyCapUtilization >= 1.0` | Morpho Blue Vault（placeholder `0x000…000`） | Aave + 40 bps 預設溢價 |

常數：`RISK_FREE_APY_FLOOR = 0.04` · `RISK_FREE_APY_CEILING = 0.052`

---

## `variational-hedge-adapter.ts`

### `buildVariationalShortOrder()`

- 鏈：`ARBITRUM_ONE_CHAIN_ID` (42161)
- 槓桿：固定 **1×** (`VARIATIONAL_TARGET_LEVERAGE`)
- 輸出：未簽名 `variational.perp.market` payload（簽名仍由 Session Key 層處理）

### `evaluateVariationalOrderbookDepth()`

移植 `evaluateHlOrderbookGapGuard()` 語義：

- Gap 窗口：`isHlOrderbookGapWindow()`（HKT 海嘯 + UTC 週末）
- 槓桿縮放：3× → 1×
- 深度地板：2× `minDepthUsd`
- Reason codes：`VARIATIONAL_ORDERBOOK_GAP_GUARD` · `VARIATIONAL_LEVERAGE_SCALE` · `VARIATIONAL_DEPTH_GUARD`

---

## 架構隔離保證

```text
[ Existing Hot Path — UNTOUCHED ]
  pkg/soil_core.wasm
  src/services/risk-control.ts
  SliverVineGate.sol

[ V1.5 Isolated Adapters — NEW ]
  aave-morpho-fallback-adapter.ts  →  Cron / yield-router 未來接線點
  variational-hedge-adapter.ts       →  Arbitrum DEX hedge-leg 擴展點
```

---

## 回歸驗證

```bash
pnpm test -- --run
```

| 指標 | 結果 |
|------|------|
| Test Files | 168 passed |
| Tests | **742 passed** |
| 回歸 | **零破壞** |

---

## 後續接線（V1.5 路線圖）

1. `yield-router-stack.ts` — 於 `checkTieredFallbackCondition()` 為 true 時呼叫 `routeToRiskFreeBase()`
2. `arbitrum-adapter.ts` — 新增 `variational` venue ID 並委派 `buildVariationalShortOrder()`
3. Morpho Blue vault 地址 — 待主網部署後替換 placeholder
4. Variational REST/WSS — 接入 `rpc-whitelist.ts` + `checkSoilResistance()` 預執行閘門

---

## 關聯 SSOT

- [`docs/architecture/CROSS_CHAIN_RISK_AND_EVOLUTION.md`](../architecture/CROSS_CHAIN_RISK_AND_EVOLUTION.md) — 階段 B 路線圖
- [`docs/architecture/TECHNICAL_SPECIFICATION.md`](../architecture/TECHNICAL_SPECIFICATION.md) — R01–R20 · Aave 基準績效費
